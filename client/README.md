# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

To run the client, execute the following command from the root of the repository:

```bash
npm install
npm run dev
```

You can then navigate to `http://localhost:5173` to view the client.